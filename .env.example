# ── Image version (tag Docker Hub, ex: latest, 0.1.0) ──
OBLIFIELD_VERSION=latest

# ── Database ──
# If using the built-in postgres container (docker-compose.yml):
DB_PASSWORD=changeme

# If using an external PostgreSQL (docker-compose.external-db.yml):
# DATABASE_URL=postgres://oblifield:yourpassword@your-db-host:5432/oblifield

# ── Server ──
PORT=3001
NODE_ENV=production
SESSION_SECRET=change-this-to-a-random-secret

# ── Client origin (for CORS — set to your actual URL) ──
CLIENT_ORIGIN=http://localhost
# LISTEN_PORT=3000

# ── App name (fallback — overridden by Settings > Company in the UI) ──
APP_NAME=Oblifield

# ── Default admin (created on first run) ──
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
